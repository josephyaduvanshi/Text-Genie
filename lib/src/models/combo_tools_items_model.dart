import 'package:flutter/material.dart';

class Items {
  final String title;
  final IconData icon;
  final Widget widget_;

  const Items({
    required this.title,
    required this.icon,
    required this.widget_,
  });
}
